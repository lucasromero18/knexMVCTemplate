<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>This is the first page</title>
  </head>
  <body>

    <h1>Book Discussion</h1>
    <a href="/newbook">Add A new book</a>
    <% for(let i = 0; i < books.length; i++){ %>
      <div class ="">
        <h1><%= books[i].name %></h1>\
        <img src="<%= books[i].img_url %>" alt="Book Cover">
        <p><%= books[i].description %></p>
        <a href="/individual/<%= books[i].id %> ">Discuss</a>
        </div>
  <%  } %>


  </body>
</html>
